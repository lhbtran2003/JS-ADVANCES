"use strict";
let str = "2";
let number = 2;
// So sánh bằng toán tử (==)
let isEqual1 = (str == number);
console.log(`So sánh lỏng (==) giữa "${strValue}" và ${numValue}: ${isEqualLoose}`);
// So sánh bằng toán tử (===)
let isEqual2 = (str === number);
console.log(`So sánh chặt (===) giữa "${strValue}" và ${numValue}: ${isEqualStrict}`);
